<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<header>
    <% include ./partials/_header.ejs %>
</header>
<body>
   <ol>
   <!-- Displaying users urls -->
   <%Object.keys(urls)%>
    <%for (let url in userUrls){%>
    <li>
        <p> ShortURL: <a href ='http://localhost:8080/u/<%= userUrls[url].shortURL %>'><%= userUrls[url].shortURL %></a> <br> LongURL: <%= userUrls[url].url %></p>
        <!-- Edit button  -->
        <button><a href = "/urls/<%=userUrls[url].shortURL%>">Edit</a></button>
        <!-- delete button -->
        <form method="POST" action="/urls/<%=userUrls[url].shortURL%>/delete"><button type="submit" >Delete</button></form>
    </li>
    <%}%>
  </ol>
</body>
</html>